{
  "name": "coffeeshout-load-test",
  "version": "1.0.0",
  "description": "CoffeeShout 부하 테스트 (Artillery + STOMP/SockJS). cs-troubleshoot 시나리오 재현/검증용.",
  "private": true,
  "scripts": {
    "test": "artillery run scenarios/baseline.yml",
    "test:ready": "artillery run scenarios/ready-toggle.yml",
    "test:racing": "artillery run scenarios/racing-tap.yml",
    "test:room-version": "artillery run scenarios/room-version-storm.yml"
  },
  "dependencies": {
    "@stomp/stompjs": "^7.0.0",
    "artillery": "^2.0.0",
    "axios": "^1.6.0",
    "sockjs-client": "^1.6.1"
  }
}
