spring:
  datasource:
    url: jdbc:mysql://mysql:3306/coffee_shout?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password: coffee-shout_123
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    aws:
      s3:
        enabled: false
  docker:
    compose:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: create
  flyway:
    enabled: false
  data:
    redis:
      host: redis
      port: 6379
      ssl:
        enabled: false

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: "health,info,metrics,prometheus"

redisson:
  enabled: false

room:
  qr:
    prefix: http://localhost:8080/join/

logging:
  level:
    coffeeshout: INFO
    org.springframework.web: INFO
